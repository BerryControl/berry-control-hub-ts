{
  "name": "transport-model",
  "version": "0.1.0",
  "description": "The transport-model defines the interface between the web UI and ther server.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
